<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ThirdActivity"
            android:exported="true"> <!-- Android 12+ 必须显式声明 -->
            <intent-filter  >
                <!-- 必须：响应“查看内容”的 Action -->
                <action android:name="android.intent.action.VIEW" />
                <!-- 必须：默认 Category（系统自动给 Intent 加） -->
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- 核心：限定仅响应 HTTPS 协议的 Uri -->
                <data android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.myapplication.ACTION_START" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.example.myapplication.MY_CATEGORY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FragmentTestActivity"/>
        <activity android:name=".SplitFragmentActivity"/>
        <activity android:name=".FilePersistenceActivity"
            android:label="File Persistence Demo"/>
        <activity android:name=".SharedPreferencesActivity"
            android:label="SharedPreferences Demo"/>
        <activity android:name=".DatabaseActivity"
            android:label="SQLite Database Demo"/>
        <activity android:name=".DatabaseAdvancedActivity"
            android:label="Advanced Query Demo"/>
        <activity android:name=".DatabaseTransactionActivity"
            android:label="SQLite Transaction Demo"/>
        <activity android:name=".ThreadActivity"
            android:label="Thread &amp; Handler Demo"/>
        <activity android:name=".ServiceActivity"
            android:label="Started Service Demo"/>
        <activity android:name=".BoundServiceActivity"
            android:label="Bound Service Demo"/>
        
        <service android:name=".MyStartedService" />
        <service android:name=".MyBoundService" />
        <service
            android:name=".MyForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- 注册广播接收者 1 -->
        <receiver android:name=".MyBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <!-- 监听的暗号 -->
                <action android:name="com.example.myapplication.MY_BROADCAST"/>
            </intent-filter>
        </receiver>

        <!-- 注册广播接收者 2 -->
        <receiver android:name=".SecondBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <!-- 监听同样的暗号 -->
                <action android:name="com.example.myapplication.MY_BROADCAST"/>
            </intent-filter>
        </receiver>
        <!-- 注册有序广播接收者 -->
        <!-- android:priority 决定优先级，数字越大优先级越高（最大1000） -->
        <receiver android:name=".HighPriorityReceiver"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="com.example.myapplication.ORDERED_BROADCAST"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".LowPriorityReceiver"
            android:exported="true">
            <intent-filter android:priority="0">
                <action android:name="com.example.myapplication.ORDERED_BROADCAST"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
